pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'D:\Software\Maven\apache-maven-3.9.6\mvn -B -DskipTests clean package'
            }
        }
    }
}
